<?php
$thread=$this->thread[0];

//get data from session
$userInfo = Zend_Auth::getInstance()->getStorage()->read();

echo "<font color='red'>Thread Title : </font><br>";
echo $this->thread[0]['threadTitle'];
echo "<br>";
echo "<font color='red'>Thread Body : </font><br>";
echo $this->thread[0]['threadBody'];
echo "<br><br>";
echo "<font color='red'>Comment : </font><br>";

if ($userInfo->isAdmin==1){//if he was admin
            echo $this->form;    

}  else {//normal user
    if ($this->thread[0]['isLocked']==0){ //if it was normal user and thread is not Locked
        
            echo $this->form;
        
    }
    
}

        for($i=0;$i<count($this->reply);$i++){

                    echo "<div>";
                    
                    for($j=0;$j<count($this->users);$j++){
                        if($this->reply[$i]['userID']==$this->users[$j]['userID']){
                           echo $this->users[$j]['userName'] ." : "; 
                        }
                        
                    }
                                       
                    echo $this->reply[$i]['replayBody']."<br/>"; 
                    echo "</div>";
                    if ($userInfo->isAdmin==1){
                            echo "<a href=".$this->baseUrl()."/threads/editreply/reply_id/".$this->reply[$i]['replayID']."/thread_id/".$thread['threadID'].">Edit</a>";
                            echo "    <a href=".$this->baseUrl()."/Replay/delete/reply_id/".$this->reply[$i]['replayID']."/thread_id/".$thread['threadID'].">Delete</a>";
                        
                    }  else {
                            if ($userInfo->userID==$this->reply[$i]['userID']){
                                echo "<a href=".$this->baseUrl()."/threads/editreply/reply_id/".$this->reply[$i]['replayID']."/thread_id/".$thread['threadID'].">Edit</a>";
                                  echo "    <a href=".$this->baseUrl()."/Replay/delete/reply_id/".$this->reply[$i]['replayID']."/thread_id/".$thread['threadID'].">Delete</a>";
                             }

                    } 
                    
             echo "<br><br>*************************************************";
     
        }

?>
